nextflow_workflow {

    name "Test Workflow INTEGRATE"
    script "subworkflows/local/integrate/main.nf"
    workflow "INTEGRATE"

    tag "subworkflows"
    tag "subworkflows_local"

    test("Should run without failures - harmony - stub") {

        options '-stub'

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = false
                input[2] = 2000
                input[3] = []
                input[4] = ['harmony']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

    test("Should run without failures - harmony") {

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = false
                input[2] = 2000
                input[3] = []
                input[4] = ['harmony']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(
                workflow.out.versions
            ).match()
            assert "X_emb" in anndata(workflow.out.integrations[0][1]).obsm
        }

    }

    test("Should run without failures - bbknn - stub") {

        options '-stub'

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = false
                input[2] = 2000
                input[3] = []
                input[4] = ['bbknn']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

    test("Should run without failures - bbknn") {

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = false
                input[2] = 2000
                input[3] = []
                input[4] = ['bbknn']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

    test("Should run without failures - combat - stub") {

        options '-stub'

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = false
                input[2] = 2000
                input[3] = []
                input[4] = ['combat']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

    test("Should run without failures - combat") {

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = false
                input[2] = 2000
                input[3] = []
                input[4] = ['combat']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

    test("Should run without failures - symphony - stub") {

        options '-stub'

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = false
                input[2] = 2000
                input[3] = []
                input[4] = ['symphony']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

    test("Should run without failures - extension mode - stub") {

        options '-stub'

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)
                ])
                input[1] = true
                input[2] = -1
                input[3] = []
                input[4] = ['harmony']
                input[5] = null
                input[6] = null
                input[7] = null
                input[8] = []
                input[9] = []
                input[10] = null
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

}
